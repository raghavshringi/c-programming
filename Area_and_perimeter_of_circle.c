#include<stdio.h>
int main()
    {
    	int r;
    	double pi=3.14,area,ci;
    	printf("\nenter r of circle:");
    	scanf("%d", &r);
    	area=pi*r*r;
    	printf("\n area of circle: %lf", area);
    	ci=2*pi*r;	
    printf("\n ci: %lf",ci);	
    return 0;	
    	}
